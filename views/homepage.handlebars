{{#each users as |user| }}
<div class="username entry-form">
  <div class="name">
    <h2>{{user.name}}</h2>
  </div>
  <div class="email">
    <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/each}}
